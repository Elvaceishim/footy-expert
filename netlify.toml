[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# SPA redirect - catch all routes and redirect to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
